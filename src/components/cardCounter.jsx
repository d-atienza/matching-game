// import { useState } from "react";

// export function increaaseCounter() {
//   // Declare a state variable to store the count
//   const [count, setCount] = useState(0);

//   // Function to handle incrementing the count
//   const incrementCount = () => {
//     setCount(count + 1);
//   };

//   return incrementCount;
// }

// export function decreaaseCounter() {
//   // Declare a state variable to store the count
//   const [count, setCount] = useState(0);

//   // Function to handle decrementing the count
//   const decrementCount = () => {
//     setCount(count + 1);
//   };

//   return decrementCount;
// }

// else if (outcome == "down" && decreaaseCounter <= 0) {
//   return alert("0 cards have been flipped!");

//   else if (outcome == "up" && increaaseCounter >= 2) {
//     return alert("2 cards already flipped!");
//   }
